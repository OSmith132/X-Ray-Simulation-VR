/// <summary>
/// Concrete FrameZone for AcrylicSheet. Unity can't add a generic MonoBehaviour directly to a GameObject, so this subclass is what gets attached in the Inspector.
/// </summary>
public class AcrylicFrameZone : FrameZone<AcrylicSheet> { }